import SwiftUI

struct ContentView: View {
    var body: some View {
        LocalWebView()
            .background(Color(red: 7 / 255, green: 15 / 255, blue: 35 / 255))
    }
}

#Preview {
    ContentView()
}
